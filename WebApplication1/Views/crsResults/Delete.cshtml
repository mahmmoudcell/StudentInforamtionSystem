@model WebApplication1.Models.crsResult

@{
    ViewData["Title"] = "Delete";
}

<h1 class="text-center my-4 text-danger">Delete Course Result</h1>

<div class="card border-danger">
    <div class="card-header bg-danger text-white">
        <h4 class="mb-0">Are you sure you want to delete this course result?</h4>
    </div>
    <div class="card-body">
        <h4>Course Result Details</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Degree)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Degree)
            </dd>
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Course)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Course.Id)
            </dd>
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Trainee)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Trainee.Id)
            </dd>
        </dl>
    </div>
    <div class="card-footer text-center">
        <form asp-action="Delete" class="d-inline">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Delete" class="btn btn-danger" />
        </form>
        <a asp-action="Index" class="btn btn-secondary ml-2">Cancel</a>
    </div>
</div>
